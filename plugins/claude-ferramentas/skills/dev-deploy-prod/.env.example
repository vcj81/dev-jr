# Configuração do dev-deploy-prod — copie para <raiz do repo alvo>/.claude/dev-deploy-prod/.env e preencha.
# NUNCA versione o .env real (fica fora do repo do plugin e do próprio projeto).
PROD_HOST=servidor:57772
PROD_USER=usuario
PROD_PASSWORD=senha
PROD_NAMESPACE=IBTECH
